# Server-side only. NEVER prefix a secret with VITE_ (Vite inlines VITE_ vars into the public bundle).
ANTHROPIC_API_KEY=__SET_ME__
AI_PROXY_ARMED=false
AI_MODEL=claude-sonnet-4-6
# Public client flag (correct VITE_ use — not a secret):
VITE_DEMO_MODE=false
